require "spec_helper"

RSpec.describe Umbrellalinter do
  it "has a version number" do
    expect(Umbrellalinter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
